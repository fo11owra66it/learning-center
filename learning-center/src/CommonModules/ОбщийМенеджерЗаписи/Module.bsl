
#Область ПрограммныйИнтерфейс

// Процедура для чтения данных из регистра сведений в набор записей управляемой формы
//
// Параметры:
//  Объект - ФормаКлиентскогоПриложения - ссылка на управляемую форму
//  ИмяРегистра - Строка - имя регистра сведений
//  Отбор - Структура - структура отбора (ключ - имя поля, значение - значение отбора)
//  ИмяРеквизитаФормы - Строка, Неопределено - имя реквизита формы для выгрузки данных
//                      если Неопределено, используется имя регистра
//
Процедура ЗагрузитьДанныеИзРегистраСведений(Форма, ИмяРегистра, Отбор, 
        ИмяРеквизитаФормы = Неопределено) Экспорт
    
    Если ИмяРеквизитаФормы = Неопределено Тогда
        ИмяРеквизитаФормы = ИмяРегистра;    
    КонецЕсли;    
    
    Запрос = СоздатьЗапросДляЧтенияРегистраСведений(ИмяРегистра, Отбор);
    РезультатЗапроса = Запрос.Выполнить();
    Выборка = РезультатЗапроса.Выбрать();
    
    НаборЗаписейФормы = УправляемаяФорма[ИмяРеквизитаФормы];
    НаборЗаписейФормы.Очистить();
    
    Пока Выборка.Следующий() Цикл
        НоваяСтрока = НаборЗаписейФормы.Добавить();
        ЗаполнитьЗначенияСвойств(НоваяСтрока, Выборка);
    КонецЦикла;    
        
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Код процедур и функций

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Код процедур и функций

#КонецОбласти
